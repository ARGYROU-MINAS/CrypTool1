

ScintillaNET Changelog

0.61
-------------------------------
Fixed bugs introduced into Text/GetText and other string properties

Fixed missing parameters from some functions (0,0 problem)

Fixed Shortcut Key problem

Added some more code to ScNote



0.60
-------------------------------

Rejiggered some Set/Get functions that lost their Set/Get prefix.
	// Sorry for this folks, I've been diddling with the autogen tool
	// and I wanted to get some homoginization in names, so now 
	// accessors that had the get/set inside the fn is now on 
	// the outside.
	

Added common styles to lexer enums.

Fixed GetString constructs to grab just the valid part of the string data.

Changed Text Property to use SetText/GetText

Added Enum-based properties/functions:
	public Enums.WhiteSpace ViewWhitespace
		// supercedes ViewWS
	
	public Enums.EndOfLine EndOfLineMode
		// supercedes EOLMode
		
	public void ConvertEOLs(Enums.EndOfLine eolMode)
		// supercedes ConvertEOLs(int)
		
	public void MarkerDefine(int markerNumber, Enums.MarkerSymbol markerSymbol )
		// supercedes MarkerDefine(int markerNumber, int markerSymbol )
		
	public Indexed.MarginType MarginType
		// workaround for .NET to support multiple indexed properties 
		// (very clever) ;)
		// usage:
		// objSC.MarginType[1] = Scintilla.Enums.MarginType.number;
		
	public void StyleSetCharacterSet(int style, Enums.CharacterSet characterSet )
		// supercedes StyleSetCharacterSet(int , int )
		
	public void StyleSetCase(int style, Enums.CaseVisible caseForce )
		// supercedes StyleSetCase(int style, int caseForce )


